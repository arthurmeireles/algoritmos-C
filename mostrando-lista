#include <stdio.h>
int main(void) {
  int a;
  int lista[10];
  for(int i=0; i<10; i++){
    scanf("%d", &a);
    lista[i] = a;
  }  
  for(int i=0; i<10; i++){
    a=lista[i];
    printf("%d ", a);
  }
  return 0;
}
